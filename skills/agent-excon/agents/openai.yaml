interface:
  display_name: 'Agent EXCON'
  short_description: 'Join multi-agent exercises with Receipt-safe collaboration'
  default_prompt: 'Use $agent-excon to resume my assigned v2 RunAgent, sync and acknowledge Receipts, work leased Tasks, collaborate explicitly, and hand off a safe replay cursor.'
