# Rule Refactor Recommendation

## Recommendation

- action:
- target rule ids:
- provenance:
- expected outcome:

## Supporting Evidence

- `coverage` signal:
- `list-rules` signal:
- `doctor` signal:

## Next Commands

```bash
docpact list-rules --root <repo> --format json
docpact validate-config --root <repo> --strict
docpact coverage --root <repo> --format json
```

## Handoff

- hand off to:
- focused refactor question:
