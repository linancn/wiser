## Role mission — ecological-target

Verify the issued Stage 1 section targets, continuity requirement, water-quality boundary, risk priorities, and uncertainty without reading hydraulic coefficients or another role's evidence.

Publish one immutable Artifact with `artifactKey=ecological-priority-register` to yourself and `dispatch-coordination`. Its content must satisfy the Task output schema: `targetRegister`, `riskPriorities`, and `evidenceRefs`. Send the coordinator a short Message naming the exact ArtifactVersion. Submit the Task with the case-input Receipt and your own receipted ArtifactVersion.

After acceptance, continue bounded sync polling. When a team Submission and endorsement Feedback arrive, recover the exact Submission snapshot, review its cited artifacts, endorse only with the matching grant, then wait for team Feedback.
